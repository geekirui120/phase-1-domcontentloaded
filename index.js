// Your code goes here
document.addEventListener('DOMContentLoaded', () => {
    // Select the element whose text needs to be updated
    const targetElement = document.getElementById('text'); // Ensure the element has the correct ID
    
    // Update its text content
    targetElement.textContent = 'This is really cool!';
  });
  